$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Redshift check for application name in stl_connection_log (redshift-awsdriver-nopass)

type: sqlv

job_id: <{ prefix.job }>/sqlv/redshift/appname/redshift-awsdriver

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: <{ prefix.payload }>/sql.rsc/redshift-stl-connection-log.sql

parameters:
  conn_id: <{ prefix.conn }>/db/redshift/awsdriver

x-lava-nocheck: joborphan
