$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: Fail - Redshift delete custard rollback

type: sqlv

job_id: <{ prefix.job }>/sqlv/redshift/custard-rollback

payload: <{ prefix.payload }>/sql.rsc/custard-rollback.sql

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

parameters:
  conn_id: <{ prefix.conn }>/db/redshift/base
  transaction: true

x-lava-nocheck: joborphan
