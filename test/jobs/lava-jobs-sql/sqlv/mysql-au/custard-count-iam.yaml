$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Aurora MySQL custard count using IAM auth

type: sqlv

job_id: <{ prefix.job }>/sqlv/mysql-au/custard-count-iam

payload: <{ prefix.payload }>/sql.rsc/custard-count.sql

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

parameters:
  conn_id: <{ prefix.conn }>/db/mysql/aurora-iam

x-lava-nocheck: joborphan
