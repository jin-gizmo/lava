$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Aurora MySQL custard count using Secrets Mamager

type: sqlc

job_id: <{ prefix.job }>/sqlc/mysql-au/custard-count-sm

payload: <{ prefix.payload }>/sql.rsc/custard-count.sql

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

parameters:
  conn_id: <{ prefix.conn }>/db/mysql/aurora-sm

x-lava-nocheck: joborphan
