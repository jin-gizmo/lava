$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Redshift custard count with GetClusterCredentials

type: sqlc

job_id: <{ prefix.job }>/sqlc/redshift/custard-count-iam

payload: <{ prefix.payload }>/sql.rsc/custard-count.sql

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

parameters:
  conn_id: <{ prefix.conn }>/db/redshift/iam

x-lava-nocheck: joborphan
