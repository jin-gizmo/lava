$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: FAIL - Redshift custard count with bad driver

type: sql

job_id: <{ prefix.job }>/sql/redshift/custard-count/bad-driver

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: <{ prefix.payload }>/sql.rsc/custard-count.sql

parameters:
  conn_id: <{ prefix.conn }>/db/redshift/bad_driver
  transaction: true

x-lava-nocheck: joborphan
