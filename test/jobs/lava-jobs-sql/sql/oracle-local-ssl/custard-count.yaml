$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Oracle custard count (local + SSL)

type: sql

job_id: <{ prefix.job }>/sql/oracle-local-ssl/custard-count

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: <{ prefix.payload }>/sql.rsc/custard-count.sql

parameters:
  conn_id: <{ prefix.conn }>/db/oracle/local-ssl
  transaction: true

x-lava-nocheck: joborphan
x-lava-test:
  result: OK
  marks:
    - local
