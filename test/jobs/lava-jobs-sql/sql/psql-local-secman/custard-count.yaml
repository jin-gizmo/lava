$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Postgres custard count (security manager auth)

type: sql

job_id: <{ prefix.job }>/sql/psql-local-secman/custard-count

payload: <{ prefix.payload }>/sql.rsc/custard-count.sql

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

parameters:
  conn_id: <{ prefix.conn }>/db/psql/local-secman
  transaction: true

x-lava-nocheck: joborphan
x-lava-test:
  result: OK
  marks:
    - local
