$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Single select from an Oracle table (no query splitting)

type: sql

job_id: <{ prefix.job }>/sql/oracle-local/select-single

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: <{ prefix.payload }>/sql.rsc/onecol-select.sql

parameters:
  conn_id: <{ prefix.conn }>/db/oracle/local
  raw: true

x-lava-nocheck: joborphan
x-lava-test:
  result: OK
  marks:
    - local
