$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Redshift get current user with IAM auth (pg8000)

type: sqli

job_id: <{ prefix.job }>/sqli/redshift-svl/current-user/iam-assume-role/pg8000

payload: >-
  SELECT CURRENT_USER;

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

parameters:
  conn_id: <{ prefix.conn }>/db/redshift/svl-iam-assume-role

x-lava-nocheck: joborphan
