$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Aurora Postgres custard count using Secrets Mamager

type: sqli

job_id: <{ prefix.job }>/sqli/mysql-au/custard-count-sm

payload: "SELECT COUNT(*) FROM <{db.common.schema}>.custard"

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

parameters:
  conn_id: <{ prefix.conn }>/db/mysql/aurora-sm

x-lava-nocheck: joborphan
