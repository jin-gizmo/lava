$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: FAIL - Circular job dispatch

type: dispatch

job_id: <{ prefix.job }>/dispatch/circular

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload:
  - <{ prefix.job }>/dispatch/circular

# No globals
globals: {}

x-lava-nocheck: joborphan

x-lava-test:
  result: FAIL
  error_pattern: "Circular dispatch not permitted"
  marks:
    - local
