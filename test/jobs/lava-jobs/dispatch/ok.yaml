$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Simple dispatch job

type: dispatch

job_id: <{ prefix.job }>/dispatch/ok

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload:
  - <{ prefix.job }>/cmd/hello-world

# No globals
globals: {}

x-lava-nocheck: joborphan

x-lava-test:
  result: OK
  marks:
    - local
