$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: FAIL - Payload is not an archive

type: pkg

job_id: <{ prefix.job }>/pkg/bad-payload-zip

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: <{ prefix.payload }>/exit.sh

parameters:
  command: exit.sh {{vars.status}}
  vars:
    status: 0

x-lava-nocheck: joborphan

x-lava-test:
  result: FAIL
  error_pattern: "Cannot unpack .*/exit.sh into .*"
  marks:
    - local
