$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: FAIL - Can't have both "main" and "command" params

type: pkg

job_id: <{ prefix.job }>/pkg/main-and-command

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: <{ prefix.payload }>/exit.zip

parameters:
  command: exit.sh {{vars.status}}
  main: exit.sh {{vars.status}}
  vars:
    status: 0

x-lava-nocheck: joborphan

x-lava-test:
  result: FAIL
  error_pattern: 'Only one of "command" or "main" \(deprecated\) allowed'
  marks:
    - local
