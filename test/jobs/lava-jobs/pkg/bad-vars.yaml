$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: FAIL - Wrong type for vars

type: pkg

job_id: <{ prefix.job }>/pkg/bad-vars

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: <{ prefix.payload }>/exit.zip

parameters:
  command: exit.sh {{vars.status}}
  vars: "Must be a dict / map here"

x-lava-nocheck: joborphan

x-lava-test:
  result: FAIL
  error_pattern: "vars must be a map/dict"
  marks:
    - local
