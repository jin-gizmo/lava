$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: FAIL - Bad Jinja syntax for command

type: pkg

job_id: <{ prefix.job }>/pkg/bad-command-jinja

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: <{ prefix.payload }>/exit.zip

parameters:
  command: "exit.sh {{"
  vars:
    status: 0

x-lava-nocheck: joborphan

x-lava-test:
  result: FAIL
  error_pattern: "Bad command or args: unexpected 'end of template'"
  marks:
    - local
