$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: FAIL - No command

type: pkg

job_id: <{ prefix.job }>/pkg/no-command

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: <{ prefix.payload }>/exit.zip

parameters:
  vars:
    status: 0

x-lava-nocheck: joborphan

x-lava-test:
  result: FAIL
  error_pattern: "Bad job parameters: No command"
  marks:
    - local
