$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Test on_success action that is not a list (invalid)

type: cmd

job_id: <{ prefix.job }>/action/bad-type

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

globals:
  global1: "g1"
  global2: "g2"

payload: >-
  echo This job has an action this not a list

# Must be a list not a dict
on_success:
  action: log

x-lava-nocheck: joborphan

x-lava-test:
  result: OK
  marks:
    - local
