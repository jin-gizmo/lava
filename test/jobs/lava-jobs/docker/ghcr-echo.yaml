$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - echo bull frog (arg handling test) - ghcr

type: docker

job_id: <{ prefix.job }>/docker/ghcr/echo

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: ghcr.io/jin-gizmo/lava/amzn2023/base

parameters:
  command: echo bull
  args:
    - frog
  docker: <{ prefix.conn }>/docker/base
  timeout: 360s

x-lava-nocheck: joborphan
