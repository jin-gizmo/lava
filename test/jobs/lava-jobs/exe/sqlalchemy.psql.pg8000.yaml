$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Use sqlalchemy with pg8000

type: exe

job_id: <{ prefix.job }>/exe/sqlalchemy/psql/pg8000

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: <{ prefix.payload }>/sqlalchemy-count.py <{ db.common.schema }>.custard

parameters:
  connections:
    db: <{ prefix.conn }>/db/psql/aurora

x-lava-nocheck: joborphan
