$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: FAIL - Bad parameters

type: exe

job_id: <{ prefix.job }>/exe/bad-params

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: <{ prefix.payload }>/exit.sh 0

parameters:
  bad: to the bone

x-lava-nocheck: joborphan

x-lava-test:
  result: FAIL
  error_pattern: "Unexpected keys: bad"
  marks:
    - local
