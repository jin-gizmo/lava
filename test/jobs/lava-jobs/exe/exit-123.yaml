$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: FAIL - Exit with status 123

type: exe

job_id: <{ prefix.job }>/exe/exit-123

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: <{ prefix.payload }>/exit.sh  {{vars.status}}

parameters:
  vars:
    status: 123

x-lava-nocheck: joborphan

x-lava-test:
  result: FAIL
  marks:
    - local
