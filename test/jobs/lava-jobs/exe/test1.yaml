$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: FAIL - Create git conn (ok), print arg+env and fail

type: exe

job_id: <{ prefix.job }>/exe/test1

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: <{ prefix.payload }>/print-args-env-fail.sh

parameters:
  args:
    - '{{realm.realm}}'
    - payload area={{realm.s3_payloads}}
  connections:
    git: <{ prefix.conn }>/git

x-lava-nocheck: joborphan
