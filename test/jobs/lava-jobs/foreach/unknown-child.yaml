$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: FAIL - foreach with an unknown child job

type: foreach

job_id: <{ prefix.job }>/foreach/unknown-child


worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: unknown-child-job

globals:
  from_the_parent: howdy

parameters:
  foreach:
    type: inline
    values:
      - a: A
        b: B
      - a: AA
        b: BB

x-lava-nocheck: joborphan

x-lava-test:
  result: FAIL
  error_pattern: "Job unknown-child-job: No such job"
  marks:
    - local
