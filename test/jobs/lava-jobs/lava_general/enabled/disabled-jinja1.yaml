$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Job dynamically disabled with a simple Jinja calculation

type: log

job_id: <{ prefix.job }>/disabled/jinja1

worker: <{ worker.main }>
enabled: '{{ 1+1 == 3 }}'
owner: <{ owner }>

payload: ignored

x-lava-nocheck: joborphan
