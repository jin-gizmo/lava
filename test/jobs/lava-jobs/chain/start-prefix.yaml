$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Start mid chain, skipping a failing child -- using job prefixes

type: chain

job_id: <{ prefix.job }>/chain/start-prefix

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload:
  - exe/exit-123
  - cmd/hello-world

parameters:
  job_prefix: <{ prefix.job }>/
  start: cmd/hello-world

x-lava-nocheck: joborphan

x-lava-test:
  result: OK
  marks:
    - local
