$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Start mid chain, skipping a failing child

type: chain

job_id: <{ prefix.job }>/chain/start-mid-chain

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload:
  - <{ prefix.job }>/exe/exit-123
  - <{ prefix.job }>/cmd/hello-world

parameters:
  start: <{ prefix.job }>/cmd/hello-world

x-lava-nocheck: joborphan

x-lava-test:
  result: OK
  marks:
    - local
