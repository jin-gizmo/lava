$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: FAIL - no jobs in payload list

type: chain

job_id: <{ prefix.job }>/chain/empty-payload

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: []

# No globals
globals: { }

x-lava-nocheck: joborphan

x-lava-test:
  result: FAIL
  error_pattern: "Job chain is empty"
  marks:
    - local
