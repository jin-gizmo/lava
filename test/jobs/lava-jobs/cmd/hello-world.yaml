$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Hello world

type: cmd

job_id: <{ prefix.job }>/cmd/hello-world

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: echo hello

parameters:
  args:
    - world

x-lava-nocheck: joborphan

x-lava-test:
  result: OK
  marks:
    - local
