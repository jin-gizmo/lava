$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Job is enabled with a dynamic Jinja

type: log

job_id: <{ prefix.job }>/log/enabled-dynamic

worker: <{ worker.main }>
owner: <{ owner }>
enabled: '{{ globals.do_it.startswith("y") }}'

payload: --

globals:
  do_it: yup

x-lava-nocheck: joborphan
x-lava-test:
  result: OK
  marks:
    - local
