$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Simple string schedule

type: log

job_id: <{ prefix.job }>/lavasched/string

worker: <{ worker.main }>
owner: <{ owner }>
enabled: true

dispatcher: localtime
schedule: '0 0 31 2 *'

payload: --

x-lava-nocheck: joborphan
