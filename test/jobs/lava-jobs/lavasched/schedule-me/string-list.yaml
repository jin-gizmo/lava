$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - List of strings schedule

type: log

job_id: <{ prefix.job }>/lavasched/string-list

worker: <{ worker.main }>
owner: <{ owner }>
enabled: true

dispatcher: localtime
schedule:
  - '0 0 30 2 *'
  - '0 0 31 2 *'

payload: --

x-lava-nocheck: joborphan
