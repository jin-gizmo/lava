$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: FAIL - Job with malformed schedule

type: log

payload: --

job_id: <{ prefix.job }>/lavasched/bad-sched

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

schedule: no-way

x-lava-nocheck: joborphan
