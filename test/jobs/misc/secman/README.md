# Secrets Manager Items

This directory contains YAML CLI templates for secrets manager items.

This repo is repo safe **provided** secret values are only references to
environment variables and not literal secrets. If that changes, something more
convoluted will be required (e.g. similar to the SSM params handling.)
