$schema: http://127.0.0.1:8000/schemas/latest/connection.schema.yaml

description: Docker basic connection (covers public repos)
owner: "<{ owner }>"

type: docker

conn_id: <{ prefix.conn }>/docker/base
enabled: true
