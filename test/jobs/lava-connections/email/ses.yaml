$schema: http://127.0.0.1:8000/schemas/latest/connection.schema.yaml

description: SES email connection
owner: "<{ owner }>"

type: email

conn_id: <{ prefix.conn }>/email/ses
enabled: true

reply_to:
  - no-one@nowhere.nohow

configuration_set: lava-test
