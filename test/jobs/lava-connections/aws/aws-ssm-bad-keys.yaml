$schema: http://127.0.0.1:8000/schemas/latest/connection.schema.yaml

description: AWS connection using creds from SSM but with invalid access keys format
owner: "<{ owner }>"

type: aws

conn_id: <{ prefix.conn }>/aws/ssm-bad-keys
enabled: true

region: ap-southeast-2
access_keys: <{ prefix.ssm }>/iam/bad-access-keys
