$schema: http://127.0.0.1:8000/schemas/latest/connection.schema.yaml

description: AWS connection with an invalid field
owner: "<{ owner }>"

type: aws

conn_id: <{ prefix.conn }>/aws/bad-field
enabled: true

# Invalid
bad: bad, Leroy Brown

region: ap-southeast-2
access_keys: <{ prefix.ssm }>/s3
