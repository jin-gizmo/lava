$schema: http://127.0.0.1:8000/schemas/latest/connection.schema.yaml

description: AWS connection using creds from SSM but with a non-existent parameter
owner: "<{ owner }>"

type: aws

conn_id: <{ prefix.conn }>/aws/ssm-missing-param
enabled: true

region: ap-southeast-2
access_keys: no-such-ssm-param
