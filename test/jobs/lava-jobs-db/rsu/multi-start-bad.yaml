$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: FAIL - Redshift unload multi starting at bad table

type: redshift_unload

job_id: <{ prefix.job }>/rsu/multi-start-bad

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: --

parameters:
  conn_id: <{ prefix.conn }>/db/redshift/base
  s3_conn_id: <{ prefix.conn }>/aws/ssm

  start: not_even_in_the_list
  stop_on_fail: false

  schema: <{ db.common.schema }>
  relation:
    - custard
    - nope

  bucket: "<{ s3.bucket.unload }>"
  prefix: "{{ utils.s3key(realm.s3_temp) }}/test/rsu/{{schema}}/{{relation}}.csv.gz."

  args:
    - CSV
    - ALLOWOVERWRITE
    - PARALLEL OFF
    - HEADER
    - GZIP

  dateformat: YYYY-MM-DD

  insecure: <{ rsu.insecure }>

x-lava-nocheck: joborphan
