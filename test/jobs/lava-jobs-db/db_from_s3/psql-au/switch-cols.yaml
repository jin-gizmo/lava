$schema: http://127.0.0.1:8000/schemas/latest/job.schema.yaml

description: OK - Aurora Postgres swotch load (with cols)

type: db_from_s3

job_id: <{ prefix.job }>/db_from_s3/psql/switch-cols

worker: <{ worker.main }>
enabled: true
owner: <{ owner }>

payload: --

parameters:
  db_conn_id: <{ prefix.conn }>/db/psql/aurora
  s3_conn_id: <{ prefix.conn }>/aws/ssm

  bucket: "{{ utils.s3bucket(realm.s3_payloads) }}"
  key: "{{ utils.s3key(realm.s3_payloads) }}/<{ prefix.payload }>/data.raw/custard100.csv"

  schema: <{ db.common.schema }>
  table: custard

  mode: switch

  args:
    - FORMAT CSV
    - DELIMITER ','
    - HEADER

  # <% from '_common/columns.jinja' import columns_custard %>
  columns: <{ columns_custard }>

x-lava-nocheck: joborphan
