# Common Resources

This directory contains build resources that are common across multiple O/S
types.  It is not used directly. The `resources` directory under each O/S is the
one that counts but they can link back to here for common stuff.
